import { SearchreportesPipe } from './searchreportes.pipe';

describe('SearchreportesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchreportesPipe();
    expect(pipe).toBeTruthy();
  });
});
